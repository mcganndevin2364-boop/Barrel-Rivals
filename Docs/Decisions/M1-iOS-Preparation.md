# M1 iPhone preparation

This is a chronological preparation record. Earlier entries describe the state at that time; read [the current iPhone guide](../../README-iPhone.md) and [Reins implementation evidence](../Plan/Reins-Lab-Implementation.md) for the installed version and current qualification status.

September 16, 2026. The user clarified that the test device is an iPhone 17 Pro running iOS 26.6.2 (user-reported). They have only the current Mac and no paid Apple Developer membership.

Host inspection found MacBookPro14,1, Intel Core i5, 8 GB RAM and macOS Ventura 13.7.8. Full Xcode is absent; xcode-select points to Command Line Tools. Unity 6000.6.0f1 iOS Build Support is installed. The Mac has approximately 19 GiB free after export, so download/expansion space must be checked before attempting an Xcode installation.

`IPhonePracticeBuilder.Export` and `Tools/run-m1.sh ios` export the saved practice scene using device SDK, IL2CPP, Metal, deployment target iOS 15+, version 0.2.0/build 2 and the existing development bundle identifier. Automatic signing is selected, with no invented team or signing credentials. The export is generated content under ignored `Builds/iOS/BarrelRivals-Practice`.

The actual Unity export succeeded with zero reported errors. `Tools/verify-ios-export.py` read the generated project and plist, checked device/ARM64 settings, application ID/version, app/framework targets and IL2CPP output. `Evidence/M1-iOS-Export.json` records the result. No Xcode native compilation, signed app/IPA, device installation or physical iPhone run has occurred. The earlier Android artifact remains separate and cannot run on iOS.

The user's available local route is a provisional Xcode 15.2/Personal Team attempt. Apple lists Xcode 15.2 for Ventura, while Unity 6000.6 recommends Xcode 16+. Documentation does not prove this exact Unity export and phone can build, pair and launch with 15.2, so actual tests are required. Apple's SDK/simulator version columns are not equivalent to its physical-device support column. A newer supported Mac/toolchain is the fallback if the local experiment fails. TestFlight requires paid membership and current upload requirements; no paid service, membership or cloud build has been purchased or provisioned.

Apple's official Xcode download page was opened for the user. No account credentials were requested in chat, entered by the agent or stored in this repository. Read README-iPhone.md for the route and primary sources. Only the export milestone is verified; the iOS platform acceptance gate remains open.

September 17 follow-up: the user completed sign-in, and the authenticated Apple download listing was verified. Xcode 15.2.xip is listed as 3.12 GB. The Mac had approximately 21.2 GiB free before downloading. Browser download actions did not yield a verified local archive, so the page was left open and the user was asked to save the archive to Downloads. No installation or native build has run. An independent read-only inspection identified roughly 5.25 GiB of regenerable content in this repaired project's Library/Bee if space is later needed; nothing was deleted. The existing iOS export must be preserved. Exact expanded Xcode and native-build space requirements remain unverified.

September 17 installation follow-up: the user downloaded `Xcode_15.2.xip` to Downloads (3,355,129,366 bytes). An unrestricted macOS `pkgutil --check-signature` returned signed Apple Software; the initial sandboxed attempt could not reach the verification service. The archive was expanded into the task's work folder, and `codesign --verify --deep --strict` passed for the resulting application. Xcode was then moved to `/Applications/Xcode.app` without overwriting an existing app. Version inspection returned Xcode 15.2, build 15C500b. The iPhoneOS 17.2 SDK exists. Installed allocated size was approximately 6.1 GiB and remaining free space approximately 12 GiB. No source, Unity cache or download archive was deleted.

`xcodebuild -license check` returned exit 69 and explicitly reported that the Xcode/Apple SDK license agreements have not been accepted. The user was asked to open Xcode, review the agreement and complete first-launch setup if they agree. No license was accepted by the agent. Native compilation remains pending this prerequisite. The first native attempt should use the exported Unity-iPhone scheme, ReleaseForRunning, generic iOS device, scoped DEVELOPER_DIR, two build jobs and disabled signing; a successful unsigned build would still not establish phone compatibility. A USB inspection did not see an iPhone, and the user was asked to connect/unlock it for the later signing/install stage.

September 18: the user completed Xcode setup. Both license and first-launch checks returned success. The scheme/generic-device build stopped before compilation because Xcode marked iOS 17.2 as not installed despite the available iPhoneOS SDK. The supported target-build path (`-target Unity-iPhone -sdk iphoneos`) reached compilation: the generated game code compiled and UnityFramework linked as an ARM64 binary (126,052,592 bytes). The complete app build then failed at both launch storyboard compilation steps with `iOS 17.2 Platform Not Installed`. No gameplay or launch-screen implementation change was made. A separate scratch `actool` test of a simple color asset also failed for lack of the runtime, so Apple's alternative plist launch-screen configuration would not eliminate the platform requirement.

Apple's `xcodebuild -downloadPlatform iOS` was started for iOS 17.2 Simulator (21C62), 7,351,124,481 bytes. The transfer is slow. With Unity confirmed closed, only this repaired project's ignored `Library/Bee` generated cache was removed, recovering approximately 3.36 GiB of actual free space. Source, saved scenes, the Xcode export, original projects, downloaded Xcode archive and compiled native output were preserved. A local process continuation watches the existing installer, verifies the installed runtime and then invokes `Tools/build-ios-native.sh` against the existing native output directory. It does not sign, provision, install on a phone or upload. `Evidence/M1-iOS-Native.json` is updated with waiting, failure or verified unsigned-app status; do not infer success from the queued retry alone.

The native runner is reproducible source; syntax validation passed. It uses the documented target build, scoped Xcode path, two jobs, hidden shell-script environment and explicit disabled signing. Its default output and logs remain ignored generated content. Read-only unsandboxed device/signing checks found zero iPhones and zero valid signing identities. The user was asked to connect/unlock/trust the phone and add their Apple Account in Xcode. These are still necessary before a signed physical-device test can establish compatibility with the user-reported iPhone/iOS version.

September 18, after storage cleanup: the user authorized resuming iPhone preparation and connected the phone. CoreDevice and xcdevice verified an iPhone 17 Pro on iOS 26.6.2 (23G90), wired, paired and available. Developer Mode is disabled and no valid code-signing identities were found. The user was asked to enable Developer Mode on the phone, restart/confirm, and add their Apple Account in Xcode so their Personal Team is available. Pairing success does not establish installation or launch compatibility. Sanitized findings are in `Evidence/M1-iOS-Device.json`; private device identifiers remain outside the repository.

The iOS 17.2 platform download was resumed with approximately 46.6 GiB available. A continuation watches that exact download process, verifies runtime availability, and reuses the existing compiled output for an unsigned build. Signing and physical installation remain separate pending steps. No game source or settings were changed during this resumption.

September 18, 21:33 local time: iOS 17.2 Simulator installation completed and its runtime was verified available. The queued unsigned native build then succeeded. The app and Unity framework are ARM64, and the generated app reports version 0.2.0/build 2. `Evidence/M1-iOS-Native.json` records binary sizes and SHA256 hashes. The app is unsigned and has no embedded provisioning profile; this is not yet an installed or playable phone build. The user subsequently opened the exported Xcode project. Its development team remains empty and needs the user’s Personal Team selected before signing.

September 18, signed installation: the user selected their Personal Team. A valid development identity and exact app-ID profile including the connected phone were verified. The actual-device scheme build timed out, but the supported target build produced a signed app. Deep/strict signature verification, entitlements, ARM64 binaries, profile expiry/phone inclusion and IPA integrity passed. Xcode devicectl installation failed with CoreDevice 12040 and mobiledevice -402652913: its DDI lacks the requested device variant.

The maintained pymobiledevice3 11.15.5 tool was installed in a task-local Python 3.12 environment using the bundled cryptography 48.0.1 library after avoiding an unnecessary native dependency build. Its standard Lockdown installation_proxy USB path installed the same signed IPA, and a targeted query verified com.barrelrivals.foundation version 0.2.0/build 2 on the phone. This did not mount a DDI, alter firmware, jailbreak the device or bypass signing. The user reported an Untrusted Developer/verification prompt on first open and was guided to trust their own Apple Account in iPhone device-management settings. Physical launch and gameplay remain pending. The current profile expires September 26, 2026 at 02:40 UTC.

September 18, successful first launch: after the development-certificate trust instructions, the user reported, “Okay it works and the app works.” This is recorded as user-confirmed app launch/basic operation, not an instrumented gameplay, latency or performance pass. Native build, development signing, installation and first launch are now demonstrated on the existing Mac/iPhone combination. The next device work is the complete practice flow, retry, interruption handling, control feel, timing, safe areas and sustained performance. Xcode’s DDI/debugger limitation remains separate from this successful personal-device playtest route.

September 19, Reins 0.4.0/build 4: the combined Classic/Reins export passed Unity validation. Xcode encountered a coordinated read wait under Documents; an identical temporary export outside Documents cleared it. The native compiler exposed numbered duplicate generated runtime files already present in the original export. Only 119 byte-identical copies were quarantined in the temporary copy, and all 1,122 canonical runtime source/header files matched installed Unity afterward. The repaired build compiled and signed. Strict/deep signature, profile/device inclusion, ARM64 binaries and IPA integrity passed. Standard USB installation succeeded and a targeted query confirmed 0.4.0/build 4. New-build manual launch, controls, persistence and performance remain pending. No game source, installed engine or original project folder was changed for the generated-export repair. `Reins-iOS-*` evidence records this version separately.
